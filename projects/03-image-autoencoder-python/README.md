# Image Autoencoder (MNIST) — Python

A dense autoencoder that reconstructs MNIST digits.

## Run
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python image_autoencoder_mnist.py
