# Global Ocean Weather (1750–1850) — Python

Aggregates ocean weather event counts by year and plots trends.

## Data
`data/ocean-weather/CLIWOC15.csv`

## Run
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python ocean_weather_1750_1850.py
