# Quantum Circuit (Hadamard) with Qiskit — Python

Builds a 1-qubit Hadamard circuit and runs it on local simulators.

## Run
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python quantum_circuit_hadamard.py
